# Centralized environment variables store for docker-compose

# CONFIG_PATH: relative to the compose project directory — run `docker compose` from emulator/ so ./Config.json resolves.
CONFIG_PATH="./Config.json"

# ACCEPT_EULA: Pass 'Y' to accept the license terms.
ACCEPT_EULA="Y"
